# SQL 回归测试计划

> **版本**: v2.9.0
> **日期**: 2026-05-05

---

## 1. 概述

本文档定义 SQLRustGo v2.9.0 的 SQL 回归测试计划，确保新版本不破坏现有 SQL 行为。

---

## 2. 回归测试套件

### 2.1 SQL Corpus

```bash
cargo test -p sqlrustgo-parser --test corpus_tests
```

**目的**: 验证 SQL 方言兼容性

**通过率目标**: ≥85%

**当前**: 92.6% (449/485)

### 2.2 TPC-H 查询回归

```bash
cargo run --bin bench-cli -- tpch bench --queries all --sf 0.1
```

**目的**: 验证决策支持查询不被破坏

**当前**: 13/22 查询通过

### 2.3 MySQL 兼容性回归

```bash
cargo test -p sqlrustgo-parser --test mysql_compat_tests
```

**目的**: 验证 MySQL 5.7 兼容性

---

## 3. 回归测试频率

| 测试 | 触发条件 | 执行者 |
|------|----------|--------|
| SQL Corpus | 每次 PR | CI |
| TPC-H | 每次 PR | CI |
| 完整回归 | 每次 RC | 人工 |

---

## 4. 回归严重程度定义

| 级别 | 定义 | 处理方式 |
|------|------|----------|
| P0 | 核心查询完全不工作 | 阻塞发布 |
| P1 | 性能下降 >20% | 阻塞发布 |
| P2 | 边界条件失败 | RC 内修复 |
| P3 | 非核心功能失败 | v2.10.0 修复 |

---

## 5. 已知回归限制

| 查询 | 当前状态 | 原因 |
|------|----------|------|
| Q17 | ❌ | 相关子查询未实现 |
| Q18 | ❌ | HAVING + 相关子查询 |

---

*本文档由 Hermes Agent 维护*
*更新日期: 2026-05-05*
